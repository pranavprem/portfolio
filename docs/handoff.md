**Current Handoff**
Integrated status: 2026-09-06. The Python portfolio is implemented, locally tested, and Dockerized. The public repository [pranavprem/portfolio](https://github.com/pranavprem/portfolio) has been created with local branch `main` and remote `origin`; the initial commit/push is the final setup step. **No live NAS/Cloudflare deployment is claimed.** No tunnel credential or NAS access was supplied.

**Continue Here**
Read [AGENTS.md](../AGENTS.md), [README.md](../README.md), [architecture.md](architecture.md), [story.md](story.md), this handoff, and [retrospective.md](retrospective.md). `CLAUDE.md` imports the canonical contributor instructions. These public documents preserve the whole account and operational context; do not ask the owner to repeat his story or reopen the private PDFs.

For a quick local preview, run `docker compose -f compose.yaml -f compose.local.yaml up -d --build --wait app` and open `http://127.0.0.1:8000/`. That exact stack has been built and run here. The README also provides a Docker-free Python setup. Tests start their own loopback server.

**Implemented**

- Eleven chapters, twelve checkpoints, four original landscapes, six fictional 0-10 stats, and eleven reversible story badges. Chapter 09 has separate Chat contribution and burnout cards. The ending is an ongoing journey, not a contact funnel.
- Goa school/college, Pune HSBC, SJSU and the overlapping Google Hardware/Nest internship, Salesforce Chat/Bots/Copilot/Agentforce, Slack API work, prompt versioning, and current enthusiasm. Hobbies and side quests are present, not merely archived.
- A pure reading-position state selector, absolute snapshots, cumulative badge prefixes, local reversible walking frames, and no idle animation. No scrolling interception, buttons, anchors, menu, form, download, or required choices.
- Complete server-rendered prose, supporting facts, every chapter's six-stat snapshot, and full badge descriptions. No-JS/resource-failure/reduced-motion modes stay readable. Mobile art is in flow; an oversized dock/theater yields to normal flow.
- Flask/Gunicorn with startup content/static validation, GET/HEAD-only routes, strict CSP, safe static serving, trusted hosts, request limits, inert Jinja projection, generic errors, and sanitized exception logging. No cookies, analytics, browser storage, database, or remote browser assets.
- Nonroot/read-only app and connector configuration, exact hash-locked dependencies, verified image digests, explicit local/tunnel Compose overrides, and a deny-by-default runtime-file build context.
- Detailed README, canonical agent instructions, complete sanitized narrative/source ledger, full approved architecture, this handoff, retrospective, automated tests, and commit-pinned/read-only GitHub CI.

**Settled Decisions**
The fixed key order is `coding`, `enthusiasm`, `vitality`, `charisma`, `automancy`, `sidequests`; the opening values are `0, 1, 1, 1, 0, 1` with no badges. `vitality` displays Health/HP. Same reading geometry and position produce the same state regardless of prior visits. Burnout retains existing badges; rewinding before a grant removes it.

The complete source ledger is `docs/story.md`, not the shorter runtime JSON. National top 0.01% Class 12 CS and the first-person 110% maximum/sole-student recollection are authorized. Exact IEEE rank differs between PDFs, so the website says national top ten. Salesforce starts in 2019 without choosing a disputed joining month. The 2,000+ hours belong only to Green Belt. Unknown grading, medical, exam-score, product-date, or confidential implementation details must not be invented.

The runtime is Python 3.13/Flask/Jinja/Gunicorn plus local CSS, one vanilla JS module, and original SVG. Node 24+, ESLint/Prettier, Playwright, and axe-core are development-only. Art sources are the four maps/favicon plus inline traveler/companion; badges use a shared decorative glyph and semantic labels, not eleven separate SVGs. No project license has been selected.

Local mode is `development`; production is `production`. HSTS defaults to `0` and may become `1` only after public TLS/redirect verification. Production loads only base + tunnel Compose, routes `pranavprem.com` to HTTP `http://app:8000` with origin Host Header `pranavprem.com`, and publishes no ports. `CLOUDFLARED_TOKEN_FILE` is an external protected file path, never a token value. The token is mounted only into connector UID/GID 65532. Actual NAS ACLs must be verified; Docker Desktop is not proof.

**Verification Record**
Environment: macOS arm64, local Python 3.13.14, Node 24.20.0/npm 11.19.0, pytest 9.1.1, Playwright 1.62.0, Docker Engine 29.7.2 / Desktop 4.89.0, Compose 5.5.0. The app image uses Python 3.13.15; cloudflared is 2026.8.3. Pins are in Dockerfile/Compose, not guessed architecture-specific tags.

| Check                                                                               | Actual Outcome                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `uv venv --python 3.13 .venv` and hash-checked `uv pip install`                     | Passed; 27 runtime/development packages installed.                                                                                                                                                                                                                                                                    |
| `npm install --ignore-scripts` and `npm ci --ignore-scripts`                        | Passed; npm reported zero vulnerabilities. Reproducible `npm ci` is the documented fresh-checkout command.                                                                                                                                                                                                            |
| `.venv/bin/pytest -m 'not browser'`                                                 | **143 passed**, no selected tests skipped.                                                                                                                                                                                                                                                                            |
| `.venv/bin/pytest -m browser --browser chromium --browser firefox --browser webkit` | **99 passed**, no selected tests skipped. Chromium 151.0.7922.34, Firefox 153.0, WebKit 26.5.                                                                                                                                                                                                                         |
| `npm run format`, `npm run lint`, Ruff lint and format checks                       | Passed for the final public artifacts; ESLint, Prettier, and Python checks are clean.                                                                                                                                                                                                                                 |
| axe-core audits                                                                     | Passed at opening, burnout, and final checkpoints in all three engines with the enforced CSP.                                                                                                                                                                                                                         |
| Responsive/reflow                                                                   | Automated 320/375/390/768/1024/1440 CSS-pixel widths, short viewports, 200% text enlargement, and 320px reflow passed. This is not native 400% zoom or a physical phone test.                                                                                                                                         |
| Input/state/fallback/privacy                                                        | Exact snapshots, equality/random rewinds, pips/badges, jumping, resize/restoration, keyboard/wheel, no-JS/resource failures, malformed projection, reduced-motion toggles, print, forced colors, selection, no idle rAF, CSP, same-origin requests, and empty storage passed.                                         |
| Visual review                                                                       | Main session inspected Chromium screenshots of desktop opening, SJSU, burnout, mobile opening, and mobile school. Original art, legible chronology, unobscured prose, and distinct scene progression confirmed in those captures.                                                                                     |
| Runtime dependency audit                                                            | `uvx pip-audit -r requirements.txt --disable-pip --no-deps`: no known vulnerabilities. This audits the pinned runtime closure, not the entire NAS or base OS.                                                                                                                                                         |
| Local Compose config/build/start                                                    | Passed `config --quiet` and `up -d --build --wait app`; healthy Gunicorn app at loopback port 8000.                                                                                                                                                                                                                   |
| App runtime isolation/content                                                       | Inspected UID/GID 10001, read-only root, all capabilities dropped, no-new-privileges, 256 MiB memory/64 PID limit, loopback-only local port. Runtime probe confirmed nonwritable app, writable `/tmp`, and exactly 14 approved files under `/srv/portfolio`, without PDFs, secrets, caches, or development artifacts. |
| Production Compose parsing                                                          | Passed with an explicitly synthetic external token-file path. No token mounted, connector authentication attempted, or NAS connection claimed.                                                                                                                                                                        |
| Connector binary                                                                    | Pinned image pulled; `--version` succeeded under UID/GID 65532, read-only root, no capabilities, no-new-privileges, and no network. This is not a connected tunnel.                                                                                                                                                   |
| Synthetic app log canary                                                            | Ordinary synthetic query/header request completed; retained container logs remained empty. Python tests separately cover sanitized application/Gunicorn exception logs. Connector retention stays disabled and its live canary is unrun.                                                                              |
| Payload budget                                                                      | All rendered HTML and approved static files total approximately 90 KB uncompressed; offline gzip estimate approximately 24 KB, including JS 3.5 KB and CSS 4.8 KB. This is a size measurement, not measured public transport/LCP/CLS or mobile CPU performance.                                                       |
| Git/PDF boundary                                                                    | Repository initialized on `main`; `.gitignore` verified for both originals, mixed-case PDF probes, environments, secrets, and development directories. Only named public artifacts may be staged.                                                                                                                     |
| GitHub CI/publication                                                               | Workflow exists and local equivalents pass; remote publication/run outcome is recorded after the initial push.                                                                                                                                                                                                        |

**Corrections And Review**
The first browser run was 93 passed / 6 failed: two layout defects reproduced in every engine. Large text overflowed headings/milestones and overlapped compact stats; a very short desktop viewport pinned an unreadable sheet. Wrapping, a font-relative responsive grid, flexible inventory, and measured theater fallback fixed both. All six focused regressions and the complete three-engine suite then passed.

A template-only callout invented an extra-credit mechanism for the 110% result. It was replaced with the supported maximum-possible wording; the regression checks rendered copy, not just JSON. The college recollection uses "among the top" rather than inventing an exact rank. Release automation describes manual effort, not total delivery lead time.

The first image-content probe found two local Python cache files. Docker negated directory patterns had re-included their descendants. Each admitted directory now has a subsequent deny rule followed by explicit file exceptions. The rebuilt image contains exactly 14 runtime files; CI repeats the boundary check after Python tests have created local caches.

Main-session code/security and architecture-conformance review covered request/static boundaries, Jinja/CSP/SVG safety, privacy and least privilege, state determinism, responsive fallback, source fidelity, runtime/developer separation, and documentation consistency. The implemented choices above reconcile draft examples without expanding the product. No unresolved local application blocker was found after the corrections. This is not an independent penetration test or institutional verification of biography.

**Remaining Work**

1. Provision the remotely managed Cloudflare tunnel and protected external token file on the NAS. Keep the token out of chat, Git, image layers, shell history, and logs. The README has the complete procedure and exact two-service commands.
2. Verify the actual NAS CPU/Compose/UID/ACL model and firewall isolation. Test connector token readability, outbound DNS/UDP/TCP 7844, and inability to reach unrelated NAS management. Do not substitute root, capabilities, or world-readable secrets.
3. Verify public DNS, TLS, HTTP redirect, origin Host routing, `/`, `/healthz`, static resources, and absence of optional injected edge JavaScript/challenges. Only then enable HSTS. No public domain access was changed or tested by this session.
4. Run physical iOS Safari/Android, real safe areas/browser chrome, native zoom, screen-reader/find-in-page, and representative-device performance checks. Automated reflow/axe/selection coverage does not establish these manual results.
5. Rehearse known-good separate-checkout/image rollback, connector reconnection, egress outage recovery, token rotation/recreation, and protected backups. A local successful replacement is not a verified production rollback.

There is no database or app data volume to migrate. `www` behavior and an open-source/art license are unchosen, not reasons to add scope. Retain the current public source/story and actual check evidence; no next task needs the owner's whole life story again.
